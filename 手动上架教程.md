# 阿语内容站手动上架教程（qawaeb-arabic）

> 仓库：`https://github.com/baveone01/qawaeb-arabic`（main，commit 8b9f246）
> 站点框架：Astro（静态生成 44 个页面）
> 建议部署：Cloudflare Pages（免费，GitHub 连动自动部署；与你的计算器站同一套链路）

---

## 第 1 步：Cloudflare Pages 连接 GitHub 并部署（约 5 分钟）

1. 打开 Cloudflare 控制台（用你的 Cloudflare 账号，之前部署过 calculadora.snapvil.com 的同一个）：

   **https://dash.cloudflare.com/**

2. 左侧进入 **Workers & Pages** → 点右上角 **Create** → 选 **Pages** → **Connect to Git**。

3. 授权选择 GitHub 后，选中仓库 **baveone01/qawaeb-arabic**（选中后会自动识别为 Astro）。

4. 构建配置（关键，务必照填）：

   | 项目 | 值 |
   |---|---|
   | Framework preset | **Astro** |
   | Build command | `npm run build` |
   | Build output directory | `dist` |
   | Root directory | 留空（代码在仓库根目录） |

5. 点 **Save and Deploy**。约 1 分钟内生成**默认域名**：

   **https://qawaeb-arabic.pages.dev**

> 部署完成后可先打开该域名确认 RTL 正常、三分类可点、文章页可读、搜索可用。

---

## 第 2 步（可选但推荐）：绑定自定义域名

如果你想让站点用 snapvil 品牌子域名（如 `qawaeb.snapvil.com`），在部署结束后页面选 **Custom domains** → **Set up a custom domain**，输入子域名，然后去你域名的 Cloudflare DNS 加一条 **CNAME**：

| 类型 | 名称 | 目标 |
|---|---|---|
| CNAME | qawaeb | qawaeb-arabic.pages.dev |

> ⚠️ 若绑定自定义域名，需要改 `astro.config.mjs` 里的 `site` 为最终域名后重新构建推送（sitemap / OG / RSS 里的绝对地址才会更新）。不改则维持 `qawaeb-arabic.pages.dev` 即可，功能不受影响。

---

## 第 3 步：Google Search Console 提交 sitemap（收录）

1. 打开 **https://search.google.com/search-console**，用能管理的 Google 账号添加资源。
2. 由于是 Cloudflare Pages，推荐 **网域资源类型**：输入 `qawaeb-arabic.pages.dev`，用 Cloudflare DNS 的 TXT 记录验证。
3. 进入资源后 → 左侧 **Sitemaps** → 提交：

   **https://qawaeb-arabic.pages.dev/sitemap.xml**

4. 顺手在 **URL 检查** 里提交首页 `https://qawaeb-arabic.pages.dev/` 手动抓取，加速收录。

---

## 第 4 步：Bing Webmaster 导入（约 1 分钟）

1. 打开 **https://www.bing.com/webmasters**（用微软账号登录）。
2. 左侧 **Import from GSC** → 授权导入刚建的 GSC 资源（含 sitemap）。
3. 等抓取；sitemap 会自动同步。

---

## 第 5 步：申请 Google AdSense（建议 1-2 周后）

1. 打开 **https://adsense.google.com/start/**，用与 GSC 相同的 Google 账号注册。
2. 填入站点 `https://qawaeb-arabic.pages.dev/`。
3. **注意合规前提**：内容站要有「关于 / 联系我们 / 隐私政策 / 免责声明」页面（本项目已内置《عن منا》《اتصل بنا》《سياسة الخصوصية》《إخلاء المسؤولية》四个页），这是 AdSense 审核的必要内容。
4. AdSense 审核通常要 **1-2 周**，且需要站点有一点流量兜底更容易过。此阶段先把 sitemap 收录和文章铺起来。

---

## 日常更新（加新文章怎么上线）

文章都是 Markdown，放在 `src/content/<legal|insurance|ai>/` 下，`npm run build` 验证通过后 `git push`，Cloudflare Pages 会自动重新构建上线——不需要再手动部署，这也是这套链路和计算器站一致的原因。

---

## 上架前自查清单

- [ ] Cloudflare 构建成功，`qawaeb-arabic.pages.dev` 可打开
- [ ] 首页三分类（法律模板/保险/AI 教程）能点进、文章页 RTL 排版正常
- [ ] 站内搜索可用
- [ ] GSC 已提交 sitemap.xml 且无抓取错误
- [ ] Bing 已同步
- [ ] 四个必备静态页（关于/联系/隐私/免责）在线上可访问（AdSense 审核前提）